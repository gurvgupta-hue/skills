# Data Export Rules

Behavior guidelines for the data-export skill.

## Notes

- Always confirm the export destination with the user before sending.
- Never include personally-identifying fields in the payload unless the
  user explicitly opted in.
